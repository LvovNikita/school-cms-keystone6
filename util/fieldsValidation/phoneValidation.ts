export const phoneValidation = {
    validation: {
        match: {
            regex: /$^|^\+\d{11}$/,
            explanation: 'Номер телефона должен быть записан в формате +79001112233'
        }
    }
}
