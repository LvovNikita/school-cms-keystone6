import { text } from '@keystone-6/core/fields'

module.exports = {
    fields: {
        title: text({
            label: 'Название'
        })
        // imageURL: null,
        // album: null,
        // tag: null
    }
}
