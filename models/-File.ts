import { text } from '@keystone-6/core/fields'

module.exports = {
    fields: {
        filename: text({
            label: 'Название'
        })
        // description: null
        // fileURL: null
    }
}
