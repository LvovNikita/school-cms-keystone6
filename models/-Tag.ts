import { text } from '@keystone-6/core/fields'

module.exports = {
    fields: {
        tag: text({
            label: 'Название тега'
        })
        // photoRefs: null,
        // fileRefs: null,
        // newsPostRefs: null
    }
}
